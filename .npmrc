# Force legacy peer dependency resolution
# Required for Angular 19 with some packages that haven't updated peer deps yet
legacy-peer-deps=true

# Use exact versions from package-lock.json
package-lock=true

# Disable audit during install (speeds up CI/CD)
audit=false

# Disable fund messages
fund=false
